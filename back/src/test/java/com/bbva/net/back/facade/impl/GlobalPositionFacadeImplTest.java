package com.bbva.net.back.facade.impl;

// import com.bbva.net.webservices.globalposition.GlobalPositionService;

public class GlobalPositionFacadeImplTest {

	private static final String DEFAULT_USER = "123";

	/*
	 * private GlobalPositionFacadeImpl globalPositionFacade; private GlobalPositionService globalPositionService;
	 * @Before public void init(){ this.globalPositionFacade = new GlobalPositionFacadeImpl(); globalPositionService =
	 * Mockito.mock(GlobalPositionService.class); this.globalPositionFacade.setGlobalPositionService(globalPositionService);
	 * }
	 * @Test public void checkGetCustomerProducts_OK() {
	 * Mockito.when(this.globalPositionService.get(Mockito.anyString())).thenReturn(new GlobalProductsDTO()); final
	 * GlobalProductsDTO globalProducts = this.globalPositionFacade.getGlobalProductsByUser(DEFAULT_USER); //invoca metodo a
	 * probar //final GlobalProducts globalProducts = this.globalPositionFacade.getGlobalProductsByUser(DEFAULT_USER);
	 * //Comprobar resultados //Assert.assertNotNull(globalProducts); //Mockito.verify(this.globalPositionService,
	 * Mockito.atLeastOnce()).get(DEFAULT_USER); }
	 */
}
